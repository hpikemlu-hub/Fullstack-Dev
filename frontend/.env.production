# Production environment variables
# In production, we'll use relative paths for same-origin requests
# Uncomment the line below if you need to explicitly set the production API URL
# VITE_API_URL=https://hpsb.online/api

# By default, the app will use relative paths (/api) in production
# This ensures the frontend communicates with the same domain

# Explicitly set to use relative paths
VITE_API_URL=/api